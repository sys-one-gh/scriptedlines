// ─────────────────────────────────────────────────────────────
// WALLS PANEL
// Displays wall types available to place on the drawing.
// Currently a placeholder — data will come from the database.
// Built with the same pattern as ProductsPanel so the swap
// to real data requires zero structural changes.
// ─────────────────────────────────────────────────────────────

function WallsPanel({ searchText }) {
  return (
    <div className="panel-placeholder">
      <p>Walls library coming soon.</p>
    </div>
  );
}

export default WallsPanel;
// ─────────────────────────────────────────────────────────────
// PARTS PANEL
// Displays individual millwork parts (gables, shelves, backs,
// toe kicks, fillers, scribes, end panels).
// Currently a placeholder — data will come from the database.
// Built with the same pattern as ProductsPanel so the swap
// to real data requires zero structural changes.
// ─────────────────────────────────────────────────────────────

function PartsPanel({ searchText }) {
  return (
    <div className="panel-placeholder">
      <p>Parts library coming soon.</p>
    </div>
  );
}

export default PartsPanel;